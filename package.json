{
  "name": "tsx-lvgl",
  "version": "0.1.0",
  "private": true,
  "description": "Typed TSX interfaces running on an on-device JavaScript runtime bound to native LVGL",
  "license": "MIT",
  "type": "module",
  "packageManager": "npm@11.17.0",
  "engines": {
    "node": "24.19.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "board:build": "./tools/dev qemu \"cd examples/esp-idf/tsx_lvgl_v1 && idf.py build\"",
    "board:reload": "tools/board-reload",
    "test": "npm run coverage",
    "test:fast": "node --test 'test-dist/tests/**/*.test.js' && node --test tests/board-reload-plan.test.mjs",
    "coverage": "npm run build && node --test --experimental-test-coverage --test-coverage-lines=100 --test-coverage-functions=100 --test-coverage-branches=100 --test-coverage-include='packages/*/dist/**/*.js' 'test-dist/tests/**/*.test.js' && node --test tests/board-reload-plan.test.mjs",
    "mutation": "npm run build && stryker run",
    "clean": "tsc -b --clean && rm -rf test-dist",
    "setup-hooks": "node scripts/install-git-hooks.mjs",
    "prepare": "npm run setup-hooks"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "@types/node": "24.3.0",
    "typescript": "5.9.2"
  },
  "overrides": {
    "qs": "6.15.3"
  }
}
